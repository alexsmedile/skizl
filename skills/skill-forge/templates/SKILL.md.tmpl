---
name: {{skill-name}}
description: |
  {{what it does, leading word first}}. Use when {{one trigger per real branch}}.
  Not for: {{near-misses that should NOT trigger}}.
# manual-only on Claude/Cursor? keep description and add:
# disable-model-invocation: true
# portable metadata belongs under metadata; add host extensions only for the selected profile.
# metadata:
#   version: "1.0.0"
---

# {{Title}}

{{One sentence: the job, and the process this skill makes predictable.}}

## Workflow

1. **{{Step}}.** {{Action.}} Done when: {{checkable, exhaustive criterion}}.
2. **{{Step}}.** {{Action.}} Done when: {{criterion}}.

## Output

{{The exact shape of what this skill produces.}}

## References
<!-- only if a branch earned one — delete this section otherwise -->

- [references/{{name}}.md](references/{{name}}.md) — read when {{activation condition}}.

## Done

{{The final completion bar — the condition that makes the whole run checkable.}}
