{
  "skill_name": "{{skill-name}}",
  "evals": [
    {
      "id": 1,
      "kind": "behavior",
      "prompt": "{{realistic user prompt — concrete and messy: file names, context, typos}}",
      "expectations": [
        "{{objective, discriminating condition with evidence available in the output}}"
      ],
      "files": []
    },
    {
      "id": 2,
      "kind": "trigger",
      "prompt": "{{near-miss prompt that should NOT trigger the skill}}",
      "should_trigger": false,
      "files": []
    }
  ]
}
